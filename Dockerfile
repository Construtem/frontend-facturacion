FROM node:22-alpine

WORKDIR /app

ARG DEV_BACKEND_URLS
ARG NEXT_PUBLIC_TOKEN_MERCADOPAGO

COPY package*.json ./

RUN npm install

COPY . .

ENV DEV_BACKEND_URLS=$DEV_BACKEND_URLS
ENV NEXT_PUBLIC_TOKEN_MERCADOPAGO=$NEXT_PUBLIC_TOKEN_MERCADOPAGO

RUN npm run build

EXPOSE 3000
    
CMD ["npm", "start"]

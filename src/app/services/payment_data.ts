// import api from './api';

export const getPaymentData = async (id: number) => {
    /*const response = await api.get(`http://localhost:8080/API/v1/post-pago/${id}`);
    return response;*/
    // este codigo aun no se esta usando
    console.log("este codigo no se deberia ejecutar aun", id);
};